VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_PASSCODE=

# Server-side only. Deliberately has no VITE_ prefix — Vite inlines any
# VITE_-prefixed var into the client bundle, which would publish the key.
ANTHROPIC_API_KEY=
